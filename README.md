# css-unit-converter
A lightweight, client-side HTML/JS utility that converts CSS values from relative vw units to absolute px units based on a user-defined screen width. Paste your CSS, set the reference width, and get a ready-to-copy CSS output in px.
